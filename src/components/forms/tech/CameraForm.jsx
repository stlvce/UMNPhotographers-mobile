import { View } from "react-native";
import { Text } from "react-native-paper";

const CamerForm = () => {
  return (
    <View>
      <Text>Камера</Text>
    </View>
  );
};

export default CamerForm;
